import { isString } from "../util/isString";
import { isObject } from "../util/isObject";
import { isAccessorChain } from "./createAccessorModelProxy";
import { hasStringAtKey } from "../util/hasKey";
let bindingCache = {};
export class Binding {
    path;
    parts;
    value;
    constructor(path) {
        this.path = path;
        this.parts = path.split(".");
        let body = "return x";
        for (let i = 0; i < this.parts.length; i++)
            body += '?.["' + this.parts[i] + '"]';
        this.value = new Function("x", body);
    }
    set(state, value) {
        const cv = this.value(state);
        if (cv === value)
            return state;
        const ns = Object.assign({}, state);
        let o = ns;
        for (let i = 0; i < this.parts.length; i++) {
            const part = this.parts[i];
            const no = i === this.parts.length - 1 ? value : Object.assign({}, o[part]);
            o[part] = no;
            o = no;
        }
        return ns;
    }
    delete(state) {
        const ns = Object.assign({}, state);
        let o = ns;
        let part;
        for (let i = 0; i < this.parts.length - 1; i++) {
            part = this.parts[i];
            const no = Object.assign({}, o[part]);
            o[part] = no;
            o = no;
        }
        part = this.parts[this.parts.length - 1];
        if (!o.hasOwnProperty(part))
            return state;
        delete o[part];
        return ns;
    }
    static get(path) {
        if (isString(path)) {
            let b = bindingCache[path];
            if (b)
                return b;
            b = new Binding(path);
            bindingCache[path] = b;
            return b;
        }
        if (isBindingObject(path))
            return this.get(path.bind);
        if (path instanceof Binding)
            return path;
        if (isAccessorChain(path))
            return this.get(path.toString());
        throw new Error("Invalid binding definition provided.");
    }
}
export function isBinding(value) {
    if (isObject(value) && hasStringAtKey(value, "bind"))
        return true;
    if (isAccessorChain(value))
        return true;
    return value instanceof Binding;
}
export function isBindingObject(value) {
    return isObject(value) && hasStringAtKey(value, "bind");
}
