import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { Widget } from "../ui/Widget";
import { TextualBoundedObject, } from "./TextualBoundedObject";
export class Ellipse extends TextualBoundedObject {
    constructor(config) {
        super(config);
    }
    declareData(...args) {
        super.declareData(...args, {
            colorIndex: undefined,
            fill: undefined,
            stroke: undefined,
        });
    }
    render(context, instance, key) {
        const { data } = instance;
        const { bounds, colorIndex } = data;
        if (!bounds.valid())
            return false;
        return (_jsxs("g", { className: data.classNames, children: [_jsx("ellipse", { className: this.CSS.element(this.baseClass, "rect", colorIndex != null && "color-" + colorIndex), cx: (bounds.l + bounds.r) / 2, cy: (bounds.t + bounds.b) / 2, rx: bounds.width() / 2, ry: bounds.height() / 2, style: data.style, fill: data.fill, stroke: data.stroke }), this.renderChildren(context, instance)] }, key));
    }
}
Ellipse.prototype.baseClass = "ellipse";
Ellipse.prototype.anchors = "0 1 1 0";
Widget.alias("ellipse", Ellipse);
