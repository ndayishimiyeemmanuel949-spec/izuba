import { jsx as _jsx } from "react/jsx-runtime";
/** @jsxImportSource react */
import { getContent, contentAppend } from "../Widget";
import { StyledContainerBase } from "../Container";
import { isArray } from "../../util/isArray";
import { isUndefined } from "../../util/isUndefined";
import { isNumber } from "../../util/isNumber";
function validContent(r) {
    let content = [];
    for (let key in r)
        if (key != "label")
            contentAppend(content, r[key]);
    return content;
}
export class LabelsTopLayout extends StyledContainerBase {
    constructor(config) {
        super(config);
    }
    init() {
        if (this.vertical && isUndefined(this.columns))
            this.columns = 1;
        super.init();
    }
    render(context, instance, key) {
        let { children, data } = instance;
        let state = {
            rows: [],
            currentRow: 0,
            labelCells: [],
            fieldCells: [],
            rowCapacities: [this.columns],
        };
        const processContent = (r) => {
            if (!r)
                return;
            if (isArray(r.content) && r.useParentLayout)
                r.content.forEach((x) => processContent(x));
            else if (r.atomic && r.type == "layout-cell")
                this.addItem(state, isArray(r.content) && r.content.length == 1 ? r.content[0] : r.content, r.data);
            else
                this.addItem(state, r, {});
        };
        children.forEach((item) => processContent(item.vdom));
        this.addRow(state);
        return (_jsx("table", { className: data.classNames, style: data.style, children: _jsx("tbody", { children: state.rows }) }, key));
    }
    addRow(state) {
        if (state.labelCells.length > 0)
            state.rows.push(_jsx("tr", { children: state.labelCells }, state.rows.length));
        if (state.fieldCells.length > 0)
            state.rows.push(_jsx("tr", { children: state.fieldCells }, state.rows.length));
        state.labelCells = [];
        state.fieldCells = [];
        state.currentRow++;
        if (state.currentRow == state.rowCapacities.length)
            state.rowCapacities.push(this.columns);
    }
    addItem(state, item, data) {
        while (state.labelCells.length == state.rowCapacities[state.currentRow])
            this.addRow(state);
        if (data.rowSpan > 1) {
            for (let row = state.currentRow + 1; row < state.currentRow + data.rowSpan; row++) {
                if (row == state.rowCapacities.length)
                    state.rowCapacities.push(this.columns);
                state.rowCapacities[row] -= data.colSpan || 1;
            }
        }
        if (data.colSpan > 1)
            state.rowCapacities[state.currentRow] -= data.colSpan - 1;
        state.labelCells.push(_jsx("td", { className: this.CSS.element(this.baseClass, "label"), colSpan: data.colSpan, children: getContent(item.label) }, state.labelCells.length));
        state.fieldCells.push(_jsx("td", { className: this.CSS.element(this.baseClass, "field"), colSpan: data.colSpan, rowSpan: isNumber(data.rowSpan) ? 2 * data.rowSpan - 1 : undefined, style: data.style, children: validContent(item) }, state.fieldCells.length));
    }
}
LabelsTopLayout.prototype.baseClass = "labelstoplayout";
LabelsTopLayout.prototype.vertical = false;
LabelsTopLayout.prototype.columns = undefined;
export class LabelsTopLayoutCell extends StyledContainerBase {
    constructor(config) {
        super(config);
    }
    declareData(...args) {
        super.declareData(...args, {
            colSpan: undefined,
            rowSpan: undefined,
        });
    }
    render(context, instance, key) {
        let { content } = this.renderChildren(context, instance);
        return {
            atomic: true,
            type: "layout-cell",
            data: instance.data,
            content,
        };
    }
}
LabelsTopLayoutCell.prototype.useParentLayout = true;
