import { Widget } from "../Widget";
import { PureContainerBase } from "../PureContainer";
export class Content extends PureContainerBase {
    constructor(config) {
        super(config);
    }
    init() {
        super.init();
        if (!this.putInto)
            this.putInto = this.for || this.name;
    }
}
Content.prototype.name = "body";
Content.prototype.isContent = true;
Widget.alias("content", Content);
