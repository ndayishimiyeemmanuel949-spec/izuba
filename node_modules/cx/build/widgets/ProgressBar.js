import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
/** @jsxImportSource react */
import { Widget } from "../ui/Widget";
import { isNumber } from "../util/isNumber";
export class ProgressBar extends Widget {
    constructor(config) {
        super(config);
    }
    declareData(...args) {
        super.declareData({
            disabled: undefined,
            text: undefined,
            value: undefined,
        }, ...args);
    }
    render(context, instance, key) {
        let { widget, data } = instance;
        let { text, value, disabled } = data;
        let { CSS, baseClass } = widget;
        if (!isNumber(value))
            value = 0;
        return (_jsxs("div", { className: CSS.expand(data.classNames, CSS.state({ disabled })), style: data.style, children: [_jsx("div", { className: CSS.element(this.baseClass, "indicator"), style: {
                        width: `${(value > 1 ? 1 : value < 0 ? 0 : value) * 100}%`,
                    } }), _jsx("div", { className: CSS.element(this.baseClass, "label"), children: text })] }, key));
    }
}
ProgressBar.prototype.styled = true;
ProgressBar.prototype.disabled = false;
ProgressBar.prototype.baseClass = "progressbar";
