import { jsx as _jsx } from "react/jsx-runtime";
/** @jsxImportSource react */
import { Widget, getContent } from "../../ui/Widget";
import { OverlayBase, OverlayBeacon, OverlayComponent, OverlayInstance, } from "./Overlay";
import { ContentPlaceholder } from "../../ui/layout/ContentPlaceholder";
import { ZIndexManager } from "../../ui/ZIndexManager";
import { Button } from "../Button";
import { parseStyle } from "../../util/parseStyle";
import { Localization } from "../../ui/Localization";
import { stopPropagation } from "../../util/eventCallbacks";
import { ddMouseDown, ddDetect, ddMouseUp } from "../drag-drop/ops";
import { isDefined } from "../../util/isDefined";
import { isString } from "../../util/isString";
export class WindowInstance extends OverlayInstance {
    headerEl;
    footerEl;
    bodyEl;
    containerEl;
}
export class Window extends OverlayBase {
    init() {
        if (isDefined(this.closeable))
            this.closable = this.closeable;
        if (isString(this.headerStyle))
            this.headerStyle = parseStyle(this.headerStyle);
        if (isString(this.footerStyle))
            this.footerStyle = parseStyle(this.footerStyle);
        if (isString(this.bodyStyle))
            this.bodyStyle = parseStyle(this.bodyStyle);
        super.init();
    }
    declareData(...args) {
        return super.declareData(...args, {
            title: undefined,
            closable: undefined,
            bodyStyle: { structured: true },
            bodyClass: { structured: true },
            headerStyle: { structured: true },
            footerStyle: { structured: true },
        });
    }
    initHelpers(...args) {
        return super.initHelpers(...args, {
            header: Widget.create(this.header || {
                type: ContentPlaceholder,
                name: "header",
                scoped: true,
            }),
            footer: Widget.create(this.footer || {
                type: ContentPlaceholder,
                name: "footer",
                scoped: true,
            }),
            close: this.closable &&
                Button.create({
                    mod: "hollow",
                    dismiss: true,
                    icon: "close",
                    style: "margin-left: auto",
                    onTouchStart: stopPropagation,
                    onMouseDown: stopPropagation,
                }),
        });
    }
    exploreCleanup(context, instance) {
        super.exploreCleanup(context, instance);
        let { helpers } = instance;
        let unregisterHeader = helpers.header && helpers.header.unregisterContentPlaceholder;
        if (unregisterHeader)
            unregisterHeader();
        let unregisterFooter = helpers.footer && helpers.footer.unregisterContentPlaceholder;
        if (unregisterFooter)
            unregisterFooter();
    }
    renderHeader(context, instance, key) {
        let { data } = instance;
        let result = [];
        if (data.title)
            result.push(data.title);
        if (instance.helpers) {
            let header = getContent(instance.helpers.header && instance.helpers.header.render(context));
            if (header)
                result.push(header);
            if (data.closable && instance.helpers.close)
                result.push(getContent(instance.helpers.close.render(context)));
        }
        return result;
    }
    renderFooter(context, instance, key) {
        return getContent(instance.helpers && instance.helpers.footer && instance.helpers.footer.render(context));
    }
    render(context, instance, key) {
        var header = this.renderHeader(context, instance, "header");
        var footer = this.renderFooter(context, instance, "footer");
        return (_jsx(OverlayBeacon, { childrenFactory: (beaconEl) => (_jsx(WindowComponent, { beaconEl: beaconEl, instance: instance, header: header, footer: footer, subscribeToBeforeDismiss: context.options.subscribeToBeforeDismiss, parentEl: context.options.parentEl, children: this.renderContents(context, instance) })) }, key));
    }
}
Window.prototype.baseClass = "window";
Window.prototype.closable = true;
Window.prototype.resizable = false;
Window.prototype.fixed = false;
Window.prototype.autoFocus = true;
Window.prototype.focusable = true;
Window.prototype.pad = true;
Window.prototype.needsBeacon = true;
Widget.alias("window", Window);
Localization.registerPrototype("cx/widgets/Window", Window);
class WindowComponent extends OverlayComponent {
    headerEl;
    footerEl;
    bodyEl;
    renderOverlayBody() {
        var { widget, data } = this.props.instance;
        var { CSS, baseClass, pad } = widget;
        let header, footer;
        if (this.props.header.length > 0) {
            header = (_jsx("header", { ref: (c) => {
                    this.headerEl = c;
                }, className: CSS.element(baseClass, "header"), style: data.headerStyle, onMouseDown: this.onHeaderMouseDown.bind(this), onMouseUp: ddMouseUp, onMouseMove: this.onHeaderMouseMove.bind(this), onTouchStart: this.onHeaderMouseDown.bind(this), onTouchEnd: ddMouseUp, onTouchMove: this.onHeaderMouseMove.bind(this), children: this.props.header }, "header"));
        }
        if (this.props.footer) {
            footer = (_jsx("footer", { ref: (c) => {
                    this.footerEl = c;
                }, className: CSS.element(baseClass, "footer"), style: data.footerStyle, children: this.props.footer }, "footer"));
        }
        var body = (_jsx("div", { ref: (c) => {
                this.bodyEl = c;
            }, className: CSS.expand(CSS.element(baseClass, "body", { pad }), data.bodyClass), style: data.bodyStyle, children: this.props.children }, "body"));
        return [header, body, footer];
    }
    getOverlayCssClass() {
        var cls = super.getOverlayCssClass();
        if (this.state.active)
            cls += " cxs-active";
        return cls;
    }
    onFocusIn() {
        super.onFocusIn();
        if (!this.state.active) {
            if (this.containerEl?.contains(document.activeElement))
                this.setZIndex(ZIndexManager.next());
            this.setState({ active: true });
        }
    }
    onFocusOut() {
        super.onFocusOut();
        if (this.state.active) {
            this.setState({
                active: false,
            });
        }
    }
    onHeaderMouseDown(e) {
        e.stopPropagation();
        ddMouseDown(e);
    }
    onHeaderMouseMove(e) {
        e.stopPropagation();
        if (!this.props.instance.widget.fixed && ddDetect(e)) {
            this.startMoveOperation(e);
        }
    }
}
