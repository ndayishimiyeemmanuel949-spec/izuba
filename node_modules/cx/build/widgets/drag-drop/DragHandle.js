import { jsx as _jsx } from "react/jsx-runtime";
/** @jsxImportSource react */
import { Widget } from '../../ui/Widget';
import { StyledContainerBase } from '../../ui/Container';
import { ddHandle } from '../drag-drop/ops';
import { isArray } from '../../util/isArray';
export class DragHandle extends StyledContainerBase {
    constructor(config) {
        super(config);
    }
    explore(context, instance) {
        if (isArray(context.dragHandles))
            context.dragHandles.push(instance);
        super.explore(context, instance);
    }
    render(context, instance, key) {
        const { data } = instance;
        return (_jsx("div", { className: data.classNames, style: data.style, onTouchStart: ddHandle, onMouseDown: ddHandle, onTouchMove: ddHandle, onMouseMove: ddHandle, onTouchEnd: ddHandle, onMouseUp: ddHandle, children: this.renderChildren(context, instance) }, key));
    }
}
DragHandle.prototype.styled = true;
DragHandle.prototype.baseClass = 'draghandle';
Widget.alias('draghandle', DragHandle);
