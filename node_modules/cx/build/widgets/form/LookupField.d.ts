import { Field, FieldInstance } from "./Field";
import { BindingInput } from "../../data/Binding";
import { DropdownConfig } from "../overlay/Dropdown";
import { AccessorChain } from "../../data/createAccessorModelProxy";
import type { CxChild, RenderingContext } from "../../ui/RenderingContext";
import type { Instance } from "../../ui/Instance";
import { FieldConfig } from "./Field";
import { Prop, BooleanProp, StringProp, StructuredProp, DataRecord } from "../../ui/Prop";
export interface LookupBinding {
    local: string | AccessorChain<any>;
    remote: string | AccessorChain<any>;
    key?: boolean;
}
/** Paged query parameters for infinite scrolling mode */
export interface LookupFieldPagedQueryParams {
    query: string;
    page: number;
    pageSize: number;
}
/** Common LookupField properties shared across all variants */
interface LookupFieldBaseConfig<TOption = any> extends FieldConfig {
    /** The opposite of `disabled`. */
    enabled?: BooleanProp;
    /** Defaults to `false`. Used to make the field read-only. */
    readOnly?: BooleanProp;
    /** Default text displayed when the field is empty. */
    placeholder?: StringProp;
    /** A list of available options. */
    options?: Prop<TOption[]>;
    /** Set to `true` to hide the clear button. Default value is `false`. */
    hideClear?: boolean;
    /** Set to `false` to hide the clear button. Default value is `true`. */
    showClear?: boolean;
    /** Set to `true` to display the clear button even if `required` is set. Default is `false`. */
    alwaysShowClear?: boolean;
    /** Base CSS class to be applied to the field. Defaults to `lookupfield`. */
    baseClass?: string;
    /** Name or configuration of the icon to be put on the left side of the input. */
    icon?: StringProp | Record<string, any>;
    /** Additional config to be applied to all items. */
    itemConfig?: any;
    /** An array of objects describing the mapping of option data to store data. */
    bindings?: LookupBinding[];
    /** A delay in milliseconds between typing stop and query. Default is `150`. */
    queryDelay?: number;
    /** Minimal number of characters required before query is made. */
    minQueryLength?: number;
    /** Set to `true` to hide the search field. */
    hideSearchField?: boolean;
    /** Number of options required to show search field. Defaults to `7`. */
    minOptionsForSearchField?: number;
    /** Text to display while data is being loaded. */
    loadingText?: string;
    /** Error message displayed if server query throws an exception. */
    queryErrorText?: string;
    /** Set to `true` to report a validation error when the selected value is not present in `options`. Only applies when `options` is an array. Default is `false`. */
    validateOptionExists?: boolean;
    /** Error message displayed when the selected value is not present in `options`. */
    invalidOptionText?: string;
    /** Message to be displayed if no entries match the user query. */
    noResultsText?: string;
    /** Name of the field which holds the id of the option. Default is `id`. */
    optionIdField?: string;
    /** Name of the field which holds the display text of the option. Default is `text`. */
    optionTextField?: string;
    /** Name of the field to store id of selected value in multiple mode. Default is `id`. */
    valueIdField?: string;
    /** Name of the field to store display text of selected value. Default is `text`. */
    valueTextField?: string;
    /** `onQuery` will be called once to fetch all options; filtering occurs client-side. */
    fetchAll?: boolean;
    /** When set with `fetchAll`, fetched options are cached for widget lifetime. */
    cacheAll?: boolean;
    /** Close the dropdown after selection. Default is `true`. */
    closeOnSelect?: boolean;
    /** Message displayed if the entered search query is too short. */
    minQueryLengthMessageText?: string;
    /** Set to `true` to sort dropdown options. */
    sort?: boolean;
    /** Additional list options, such as grouping configuration, custom sorting, etc. */
    listOptions?: Record<string, any>;
    /** Show dropdown immediately after component mount; useful for cell editing. */
    autoOpen?: BooleanProp;
    /** Allow enter key events to propagate; useful for forms or grid cell editors. */
    submitOnEnterKey?: BooleanProp;
    /** Allow dropdown enter key events to propagate for form submission. */
    submitOnDropdownEnterKey?: BooleanProp;
    /** Allow quick selection of all displayed items on `Ctrl + A` key combination. */
    quickSelectAll?: boolean;
    /** Parameters that affect filtering. */
    filterParams?: StructuredProp;
    /** Callback to create a filter function for given filter params. */
    onCreateVisibleOptionsFilter?: string | ((filterParams: any, instance: Instance) => (option: TOption) => boolean);
    /** Additional configuration to be passed to the dropdown, such as `style`, `positioning`, etc. */
    dropdownOptions?: Partial<DropdownConfig>;
    /** Custom validation function. */
    onValidate?: string | ((value: number | string, instance: Instance, validationParams: Record<string, unknown>) => unknown);
}
/** Props for infinite mode: uses onQueryPage */
interface LookupFieldInfiniteProps<TOption = any> {
    /** Enable infinite scrolling. */
    infinite: true;
    /** Number of items per page. Default is `100`. */
    pageSize?: number;
    /** Query function for infinite mode. */
    onQueryPage: string | ((params: LookupFieldPagedQueryParams, instance: Instance) => TOption[] | Promise<TOption[]>);
}
/** Props for standard mode: uses onQuery */
interface LookupFieldStandardProps<TOption = any> {
    /** Standard mode (no infinite scrolling). */
    infinite?: false;
    onQuery?: string | ((query: string, instance: Instance) => TOption[] | Promise<TOption[]>);
}
/** Props for multiple selection mode */
interface LookupFieldMultipleProps<TRecord = any> {
    /** Enable multiple selection. */
    multiple: true;
    /** A list of selected ids. */
    values?: Prop<(number | string)[]>;
    /** A list of selected records. */
    records?: Prop<TRecord[]>;
    /** Custom display text for records. */
    onGetRecordDisplayText?: ((record: TRecord, instance: Instance) => string) | null;
}
/** Props for single selection mode */
interface LookupFieldSingleProps {
    /** Single selection (default). */
    multiple?: false;
    /** Selected value. */
    value?: Prop<number | string | null>;
    /** Text associated with the selection. */
    text?: StringProp;
}
type LookupFieldInfiniteMultipleConfig<TOption = any, TRecord = any> = LookupFieldBaseConfig<TOption> & LookupFieldInfiniteProps<TOption> & LookupFieldMultipleProps<TRecord>;
type LookupFieldInfiniteSingleConfig<TOption = any> = LookupFieldBaseConfig<TOption> & LookupFieldInfiniteProps<TOption> & LookupFieldSingleProps;
type LookupFieldStandardMultipleConfig<TOption = any, TRecord = any> = LookupFieldBaseConfig<TOption> & LookupFieldStandardProps<TOption> & LookupFieldMultipleProps<TRecord>;
type LookupFieldStandardSingleConfig<TOption = any> = LookupFieldBaseConfig<TOption> & LookupFieldStandardProps<TOption> & LookupFieldSingleProps;
export type LookupFieldConfig<TOption = any, TRecord = any> = LookupFieldInfiniteMultipleConfig<TOption, TRecord> | LookupFieldInfiniteSingleConfig<TOption> | LookupFieldStandardMultipleConfig<TOption, TRecord> | LookupFieldStandardSingleConfig<TOption>;
export interface LookupFieldUniversalConfig<TOption = any, TRecord = any> extends LookupFieldBaseConfig<TOption>, Omit<Partial<LookupFieldSingleProps>, "multiple">, Omit<Partial<LookupFieldMultipleProps<TRecord>>, "multiple">, Omit<Partial<LookupFieldStandardProps<TOption>>, "infinite">, Omit<Partial<LookupFieldInfiniteProps<TOption>>, "infinite"> {
    multiple?: boolean;
    infinite?: boolean;
}
export declare class LookupField<TOption = any, TRecord = any> extends Field<LookupFieldConfig<TOption, TRecord>> {
    baseClass: string;
    multiple: boolean;
    hideClear?: boolean;
    showClear: boolean;
    alwaysShowClear: boolean;
    hideSearchField: boolean;
    minOptionsForSearchField: number;
    loadingText: string;
    queryErrorText: string;
    validateOptionExists: boolean;
    invalidOptionText: string;
    noResultsText: string;
    optionIdField: string;
    optionTextField: string;
    valueIdField: string;
    valueTextField: string;
    fetchAll: boolean;
    cacheAll: boolean;
    closeOnSelect: boolean;
    minQueryLengthMessageText: string;
    sort?: boolean;
    listOptions?: Record<string, any> | null;
    autoOpen?: boolean;
    submitOnEnterKey?: boolean;
    submitOnDropdownEnterKey?: boolean;
    pageSize: number;
    infinite?: boolean;
    quickSelectAll?: boolean;
    queryDelay: number;
    minQueryLength: number;
    onGetRecordDisplayText?: ((record: Record<string, any>, instance: Instance) => string) | null;
    onQuery?: string | ((query: string, instance: Instance) => Promise<Record<string, any>[]> | Record<string, any>[]);
    onQueryPage?: string | ((params: LookupFieldPagedQueryParams, instance: Instance) => Promise<Record<string, any>[]> | Record<string, any>[]);
    onCreateVisibleOptionsFilter?: string | ((filterParams: unknown, instance: Instance) => (option: Record<string, any>) => boolean);
    value?: BindingInput;
    text?: BindingInput<string>;
    records?: Record<string, any>[];
    values?: unknown[];
    options?: Record<string, any>[];
    enabled?: boolean;
    placeholder?: string;
    readOnly?: boolean;
    dropdownOptions?: Partial<DropdownConfig>;
    bindings?: BindingConfig[];
    keyBindings?: BindingConfig[];
    itemConfig?: CxChild;
    declareData(...args: Record<string, any>[]): void;
    init(): void;
    prepareData(context: RenderingContext, instance: FieldInstance<LookupField>): void;
    renderInput(context: RenderingContext, instance: FieldInstance<LookupField>, key: string): React.ReactNode;
    filterOptions(instance: Instance, options: DataRecord[], query?: string): DataRecord[];
    isEmpty(data: Record<string, any>): boolean;
    getValidationValue(data: Record<string, any>): unknown;
    formatValue(context: RenderingContext, instance: Instance): string | React.ReactNode;
}
interface BindingConfig {
    local: string;
    remote: string;
    key?: boolean;
    set?: (value: unknown, instance: Instance) => void;
}
export {};
//# sourceMappingURL=LookupField.d.ts.map