import { jsx as _jsx } from "react/jsx-runtime";
/** @jsxImportSource react */
import { FocusManager } from "../../ui/FocusManager";
import { coalesce } from "../../util/coalesce";
import { isArray } from "../../util/isArray";
import { HtmlElement } from "../HtmlElement";
export class Label extends HtmlElement {
    constructor(config) {
        super(config);
    }
    declareData(...args) {
        super.declareData(...args, {
            required: undefined,
            disabled: undefined,
            htmlFor: undefined,
            asterisk: undefined,
        });
    }
    prepareData(context, instance) {
        let { data } = instance;
        data.stateMods = {
            ...data.stateMods,
            disabled: data.disabled,
        };
        data._disabled = data.disabled;
        super.prepareData(context, instance);
    }
    explore(context, instance) {
        let { data } = instance;
        if (!data.htmlFor)
            data.htmlFor = context.lastFieldId;
        data.disabled = data.stateMods.disabled = coalesce(context.parentStrict ? context.parentDisabled : null, data._disabled, context.parentDisabled);
        data.asterisk = context.parentAsterisk || data.asterisk;
        if (instance.cache("disabled", data.disabled) || instance.cache("asterisk", data.asterisk)) {
            instance.markShouldUpdate(context);
            this.prepareCSS(context, instance);
        }
        super.explore(context, instance);
    }
    isValidHtmlAttribute(attrName) {
        switch (attrName) {
            case "asterisk":
            case "required":
                return false;
        }
        return super.isValidHtmlAttribute(attrName);
    }
    attachProps(context, instance, props) {
        super.attachProps(context, instance, props);
        let { data } = instance;
        if (data.htmlFor) {
            props.htmlFor = data.htmlFor;
            if (!props.onClick)
                props.onClick = () => {
                    //additional focus for LookupFields which are not input based
                    let el = document.getElementById(instance.data.htmlFor);
                    if (el)
                        FocusManager.focusFirst(el);
                };
        }
        if (!props.id && data.htmlFor)
            props.id = `${data.htmlFor}-label`;
        if (data.required && data.asterisk) {
            if (!isArray(props.children))
                props.children = [props.children];
            const children = props.children;
            children.push(" ");
            children.push(_jsx("span", { className: this.CSS.element(this.baseClass, "asterisk"), children: "*" }, "asterisk"));
        }
    }
}
Label.prototype.baseClass = "label";
Label.prototype.tag = "label";
Label.prototype.asterisk = false;
